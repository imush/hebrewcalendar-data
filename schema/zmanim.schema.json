{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hebrewcalendar.net/schemas/zmanim.schema.json",
  "title": "Zmanim base labels",
  "description": "Astronomical / halachic time-of-day noun labels used by consumers to render zmanim tables. Context-dependent compound labels (talit/tefilin selection, method-name interpolation, etc.) stay per-consumer since they combine these bases with per-repo formatting logic.",
  "type": "object",
  "propertyNames": { "pattern": "^[A-Z][A-Z0-9_]*$" },
  "additionalProperties": {
    "type": "object",
    "additionalProperties": false,
    "required": ["en", "he", "ru", "fr"],
    "properties": {
      "en": { "type": "string", "minLength": 1 },
      "he": { "type": "string", "minLength": 1 },
      "ru": { "type": "string", "minLength": 1 },
      "fr": { "type": "string", "minLength": 1 }
    }
  }
}
